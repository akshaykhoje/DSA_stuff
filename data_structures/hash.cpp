#include <bits/stdc++.h>
using namespace std;

int main()
{
  map<string, int> m;
  m.insert(pair<string, int>("akshay", 105));
  m.insert(pair<string, int>("mayur", 43));
  m.insert(pair<string, int>("harshmohan", 7));

  
  return 0;
}